#include<stdio.h>
#include<conio.h>
main()
{
	int num;
	printf("enter the number");
	scanf("%d",&num);
	if(num>0)
	{
		printf("the num is poisitive");
	}
	else
	{
		printf("the num  is negative");
	}
//	getch();
}
